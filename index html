<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Tourism Mini-Quiz</title>
<style>
:root { --ok:#0a7c2f; --bad:#b00020; --ink:#1b1b1b; --muted:#6b7280; --card:#fff; }
body{font-family:system-ui,Segoe UI,Roboto,Arial,sans-serif;background:#f6f7fb;color:var(--ink);margin:0;padding:2rem}
.wrap{max-width:860px;margin:auto}
h1{margin:0 0 .25rem} .sub{color:var(--muted);margin:.25rem 0 1.25rem}

.q{background:var(--card);border:1px solid #e6e6ef;border-radius:14px;padding:1rem;margin:0 0 1rem 0}
.topic{display:inline-block;font-size:.75rem;letter-spacing:.05em;text-transform:uppercase;background:#eef7ff;color:#0b5ad9;padding:.25rem .5rem;border-radius:6px;margin-bottom:.5rem}
.q h3{margin:.4rem 0 .6rem;font-size:1.05rem}
.opts{display:grid;gap:.45rem;margin:.4rem 0 .6rem}
.opts label{display:flex;align-items:flex-start;gap:.55rem;cursor:pointer;padding:.45rem .55rem;border-radius:10px}
.opts label:hover{background:#fafafe}
.bar{display:flex;gap:.6rem;align-items:center;flex-wrap:wrap}
.btn{appearance:none;border:0;background:#0b5ad9;color:#fff;border-radius:10px;padding:.6rem .9rem;font-weight:600;cursor:pointer}
.msg{min-height:1.4em;font-weight:700}
.ok{color:var(--ok)} .bad{color:var(--bad)} .muted{color:var(--muted)}
.feedback{margin:.35rem 0 0;font-size:.95rem;color:#374151}
.score{color:var(--muted);font-weight:700;margin-top:.75rem}

/* Big success text overlay */
#hooray{
position:fixed;inset:0;display:grid;place-items:center;pointer-events:none;
opacity:0;font-weight:900;font-size:clamp(2.5rem,8vw,6rem);
color:transparent;background:linear-gradient(90deg,#ff0077,#ff9800,#ffd400,#00c853,#00bcd4,#7c4dff);
-webkit-background-clip:text;background-clip:text;filter:drop-shadow(0 6px 18px rgba(0,0,0,.18));
}
#hooray.show{animation:pop 1.1s ease-out forwards,hue 6s linear infinite}
@keyframes pop{0%{transform:scale(.2) rotate(-6deg);opacity:0;letter-spacing:-.06em}
45%{transform:scale(1.1) rotate(2deg);opacity:1;letter-spacing:.02em}
100%{transform:scale(1) rotate(0);opacity:1}}
@keyframes hue{from{filter:hue-rotate(0deg) drop-shadow(0 6px 18px rgba(0,0,0,.18))}
to{filter:hue-rotate(360deg) drop-shadow(0 6px 18px rgba(0,0,0,.18))}}

/* Minimal confetti */
.confetti{position:fixed;left:0;top:0;width:100%;height:100%;pointer-events:none;overflow:hidden}
.piece{position:absolute;width:8px;height:14px;opacity:0;transform:translateY(-20px) rotate(0);
animation:fall 900ms ease-out forwards}
@keyframes fall{0%{opacity:1}100%{transform:translateY(110vh) rotate(720deg);opacity:0}}
</style>
</head>
<body>
<div class="wrap">
<h1>Tourism Mini-Quiz</h1>
<p class="sub">Pick an answer → <strong>Check</strong>. Wrong picks are cleared so you can try again. Five topics.</p>

<!-- Q1: Ecotourism -->
<section class="q" data-answer="b">
<div class="topic">Ecotourism</div>
<h3>Which option best defines <em>ecotourism</em>?</h3>
<div class="opts">
<label><input type="radio" value="a" data-fb="Ecotourism isn’t just visiting nature; it must minimise impact."> Visiting any park regardless of impact</label>
<label><input type="radio" value="b" data-fb-ok="Correct: ecotourism conserves the environment and benefits local communities."> Travel to natural areas that <strong>conserves the environment</strong> and <strong>benefits locals</strong></label>
<label><input type="radio" value="c" data-fb="Luxury alone doesn’t make a trip eco-friendly."> Luxury safaris only</label>
<label><input type="radio" value="d" data-fb="Mass tourism often increases pressure on fragile sites."> Mass tourism to beaches</label>
</div>
<div class="bar"><button class="btn">Check</button><span class="msg"></span></div>
<div class="feedback" aria-live="polite"></div>
</section>

<!-- Q2: Sustainable Tourism -->
<section class="q" data-answer="c">
<div class="topic">Sustainable Tourism</div>
<h3>Which practice best aligns with <em>sustainable tourism</em>?</h3>
<div class="opts">
<label><input type="radio" value="a" data-fb="Unmanaged rentals can raise prices and strain communities."> No local regulation for short-term rentals</label>
<label><input type="radio" value="b" data-fb="Single-use plastics increase waste; sustainability reduces it."> Heavy reliance on single-use plastics</label>
<label><input type="radio" value="c" data-fb-ok="Correct: fair wages + waste reduction + local ownership are sustainability pillars."> Community-owned lodges, fair wages, waste reduction</label>
<label><input type="radio" value="d" data-fb="Overbooking is a revenue tactic, not a sustainability practice."> Overbooking flights to maximize yield</label>
</div>
<div class="bar"><button class="btn">Check</button><span class="msg"></span></div>
<div class="feedback" aria-live="polite"></div>
</section>

<!-- Q3: Cultural Heritage Tourism -->
<section class="q" data-answer="a">
<div class="topic">Cultural Heritage</div>
<h3>Which is the best example of <em>cultural heritage tourism</em>?</h3>
<div class="opts">
<label><input type="radio" value="a" data-fb-ok="Correct: learning at UNESCO/heritage sites is core heritage tourism."> Visiting a UNESCO-listed fortress to learn its history</label>
<label><input type="radio" value="b" data-fb="Shopping is retail tourism, not heritage."> Shopping at a duty-free mall</label>
<label><input type="radio" value="c" data-fb="Theme parks are entertainment, not heritage."> Riding a roller coaster</label>
<label><input type="radio" value="d" data-fb="Jet skis are beach recreation, not heritage."> Renting a jet ski</label>
</div>
<div class="bar"><button class="btn">Check</button><span class="msg"></span></div>
<div class="feedback" aria-live="polite"></div>
</section>

<!-- Q4: Hospitality Operations -->
<section class="q" data-answer="d">
<div class="topic">Hospitality</div>
<h3>In a hotel, which department primarily handles reservations & check-in?</h3>
<div class="opts">
<label><input type="radio" value="a" data-fb="Housekeeping handles rooms & cleanliness, not reservations."> Housekeeping</label>
<label><input type="radio" value="b" data-fb="F&B manages restaurants and bars."> Food & Beverage</label>
<label><input type="radio" value="c" data-fb="Engineering maintains facilities."> Maintenance/Engineering</label>
<label><input type="radio" value="d" data-fb-ok="Correct: the Front Office (Front Desk) does reservations & arrivals."> Front Office / Front Desk</label>
</div>
<div class="bar"><button class="btn">Check</button><span class="msg"></span></div>
<div class="feedback" aria-live="polite"></div>
</section>

<!-- Q5: Aviation -->
<section class="q" data-answer="b">
<div class="topic">Aviation</div>
<h3>For international leisure travel, which document is generally required?</h3>
<div class="opts">
<label><input type="radio" value="a" data-fb="A hotel booking helps the trip, but isn’t a border document."> Hotel booking confirmation</label>
<label><input type="radio" value="b" data-fb-ok="Correct: a valid passport is required to cross most borders."> Valid passport</label>
<label><input type="radio" value="c" data-fb="Old boarding passes have no immigration use."> Old boarding pass</label>
<label><input type="radio" value="d" data-fb="Driver’s licences are domestic ID, not international travel docs."> Driver’s licence</label>
</div>
<div class="bar"><button class="btn">Check</button><span class="msg"></span></div>
<div class="feedback" aria-live="polite"></div>
</section>

<p class="score">Score: <span id="score">0</span>/5</p>
</div>

<!-- Success overlay + tiny confetti container -->
<div id="hooray">YAY! HOORAY! 🎉</div>
<div id="confetti" class="confetti" aria-hidden="true"></div>

<script>
const hooray = document.getElementById('hooray');
const scoreEl = document.getElementById('score');
const confettiBox = document.getElementById('confetti');
let score = 0;

function confettiBurst() {
const colors = ['#ff004c','#ff9800','#ffd400','#00c853','#00bcd4','#7c4dff'];
for (let i = 0; i < 90; i++) {
const p = document.createElement('div');
p.className = 'piece';
p.style.left = Math.random()*100 + 'vw';
p.style.top = '-2vh';
p.style.background = colors[(Math.random()*colors.length)|0];
p.style.transform = `translateY(-20px) rotate(${Math.random()*90}deg)`;
p.style.animationDuration = (700 + Math.random()*900) + 'ms';
confettiBox.appendChild(p);
setTimeout(() => p.remove(), 1200);
}
}

// One handler per question
document.querySelectorAll('.q').forEach((card) => {
const correct = card.dataset.answer;
const btn = card.querySelector('.btn');
const msg = card.querySelector('.msg');
const fb = card.querySelector('.feedback');

btn.addEventListener('click', () => {
const picked = card.querySelector('input[type="radio"]:checked');
if (!picked) { msg.textContent = 'Choose an option.'; msg.className='msg muted'; fb.textContent=''; return; }

const value = picked.value;
const ok = value === correct;

if (ok) {
msg.textContent = 'YAY! HOORAY! ✅'; msg.className = 'msg ok';
fb.textContent = picked.dataset.fbOk || 'Great job!';
if (!card.dataset.done) { score++; scoreEl.textContent = score; card.dataset.done = '1'; }
hooray.classList.add('show'); confettiBurst();
setTimeout(() => hooray.classList.remove('show'), 1400);
} else {
msg.textContent = '☹️ Not quite — try again!'; msg.className = 'msg bad';
fb.textContent = picked.dataset.fb || 'Review the concept and try again.';
// CLEAR the wrong selection so they can try again immediately
picked.checked = false;
}
});
});
</script>
</body>
</html>
